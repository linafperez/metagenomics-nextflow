// Limit physical task submission without adding scientific dependencies to the DAG.
// In particular, MEGAHIT and metaSPAdes retain identical inputs and remain
// independent branches, but their large temporary trees cannot run concurrently.
executor.queueSize = 1
