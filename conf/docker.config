docker {
    enabled    = true
    // Preserve host ownership for published files and cleanupable work trees.
    runOptions = '-u $(id -u):$(id -g)'
}

conda.enabled       = false
apptainer.enabled   = false
singularity.enabled = false
